re元字符
1.通配符.
2.^表示起始位置开始匹配
3.$表示在结尾位置匹配
4.*表示重复匹配，零个或者多个
5.+表示一个或者多个
6.?表示零个或者一个
7.{x}表示匹配x次，{m,n}表示匹配m-n次，但是如果都满足的时候就选最多的匹配（贪婪匹配）
总结：*等价于{0，我穷次}，+等价于{1，无穷次}，？等价于{0，1}，{1，}等价于{1，我穷次}
8.惰性匹配只需要在后面加？就行
9.[]字符集 [ab]里面的表示a或b
10.-在[]表示一个范围内
11.^在[]内表示取非的意思
12.\
 1.\d匹配所有数字 等价于[0-9]
 2.\D匹配所有的非数字 等价于[^0-9]
 3.\s匹配任何空白字符 等价于[\t\n\r\f\v]
 4.\S匹配任何非空白字符 等价于[^\t\n\r\f\v]
 5.\w匹配任何字母数字 等价于[0-9a-zA-Z]
 6.\W匹配任何非字母数字 等价于[^0-9a-zA-Z]
 7.\b匹配一个特殊边界 eg:$ # , . 空格等

13.\加上元字符取消特殊功能比如\.表示. \\表示\